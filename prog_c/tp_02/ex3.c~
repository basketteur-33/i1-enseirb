#include <stdlib.h>
#include <stdio.h>
#include <string.h>


void usage(char *s) {
  perror(s);
  exit(1);
}


int main(int argc, char *argv[]) {
  if (argc != 2)
    usage("usage: ./ex3 chaine");

  return EXIT_SUCCESS;
}
